eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8765

spring:
  application:
    name: service-feign

feign:
  hystrix:
    enabled: true #Feign是自带断路器的，在D版本的Spring Cloud之后，它没有默认打开
